export interface packageDTO {
    id: number,
    name: string,
    description?: string,
    amount: string,
    createdAt: string,
    updatedAt: string,
    createdBy: string,
    updatedBy: string
}